# Installed nmap module with 
#  pip install python-nmap

import nmap

scanner = nmap.PortScanner()

print("Welcome, this is a simple nmap automation tool")
print("<----------------------------------------------------")

# Don't copy the code while following the video,
# follow the video then write the code while trying to understand what's going on !